.d-flex.mb-1
  .btn-group.flex-shrink-0
    button.btn.btn-primary.pe-2 Open #{kind == "folder" ? "Project" : "File"}
    button.btn.btn-primary.px-2.dropdown-toggle.dropdown-toggle-split(data-bs-toggle="dropdown" data-bs-reference="parent")
      span.visually-hidden Toggle Dropdown
    .dropdown-menu.dropdown-menu-end
      li: button.dropdown-item New #{kind == "folder" ? "Project" : "File"}
  #fileNameLbl.text.flex-grow-1.overflow-ellipsis No #{kind == "folder" ? "project" : "file"} open.